{
  "name": "release-console",
  "version": "1.0.0",
  "description": "Internal deploy dashboard sample app for the GCP IAP x Descope Federated Apps blog post. Zero auth code by design — authentication and authorization happen entirely upstream, in Google Cloud IAP, before a request ever reaches this container.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "audit-auth": "bash scripts/audit-auth.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.9.0",
    "express": "^4.19.2"
  }
}
